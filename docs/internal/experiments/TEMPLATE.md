# 实验档案：<实验名>

> 实验纪律（SKILL.md §8）：引导段 / 协议文本 / persona 类改动合入前必须跑对照实验——同任务书、n≥3、判定标准先写后跑。一实验一档；判定标准在首跑前预注册，跑后不得改判；结论只陈述已验证事实。

- **日期**：
- **执行人**：
- **对象改动**：（persona / 引导段 / 协议文本；指向 commit hash 或文件行）
- **对照组 / 实验组**：

## 假设

<一句话可证伪假设：改动 X 会使指标 Y 变化 Z>

## 设置

<变量控制：除被测变量外两版完全一致——同一批任务书、同一驱动模型、同一会话深度；记录环境版本（宿主 / preset / 驱动模型）>

## n

<每组样本数，≥3；列出任务书清单或编号>

## 判定标准（预注册）

<合入 / 回退阈值。必须在首跑前写定>

- 主指标：
- 次指标：
- 探针协议（行为类断言用）：子代理逐字引用首尾行

## 原始产出路径

<子代理回复、transcript、日志的落盘路径；原始数据保留，结论可回溯>

## 结论

<数据 + 与判定标准的逐项比对；无法核实之处标为待验证>

## 决策

<合入 / 回退 / 需加跑；若加跑，新假设指向下一份档案>
